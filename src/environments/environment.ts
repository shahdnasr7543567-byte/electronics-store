/**
 * Development environment configuration. Swapped for
 * `environment.prod.ts` in production builds via `fileReplacements`
 * in `angular.json`.
 */
export const environment = {
  production: false,
  apiUrl: 'https://dummyjson.com',
};
